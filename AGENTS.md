# Agent 工作规则

## 最高优先级规则（必须遵守）

1. 如果用户描述的问题不清楚，不要盲目开始广泛寻找解决方法——先停下思考，向用户问清楚（复现步骤、具体表现、期望行为等），再动手。
2. 当用户要求参考其他 mod 的案例时，优先参考已经验证成功的案例（如项目内已有实现、用户明确指出的可参考方块/类），不要盲目去翻游戏底层源码。

## 复杂问题：证据优先，多与用户合作（最高优先级，必须遵守）

遇到以下任一情况，立即停止自行推理，向用户提问并等待回复，不要继续空转：

- 遇到矛盾信息，陷入自我怀疑；
- 连续 2 次推断被日志、源码或运行时表现推翻，思考陷入死胡同；
- 缺少关键运行时信息（崩溃日志、报错堆栈、复现步骤、客户端表现）；
- 需要验证 Minecraft / NeoForge / Create / CC:Tweaked 的运行时行为才能确定方案。

处理流程：

1. 先收集证据：读日志、读相关源码、参考其他 mod 的同类型实现（用 `minecraft-mod-source-lookup` 技能）。
2. 证据不足时，向用户提出 1~3 个具体、可操作的测试请求（例如「请进游戏验证 X，并告诉我 Y」），然后等待回复。
3. 在复现问题或与用户确认根因之前，不修改代码，不做「先试试」式修复。
4. 必要时先写最小调试代码，请用户进游戏做针对性测试和信息收集，再根据结果定位问题。

## 工作原则

- 不要过早确定解决方案；收集到足够的具体信息之后再开始解决问题。
- 重大改动前，先说明将要改动的文件与原因，征得用户同意。
- 参考其他 mod 源码解决问题时，记录参考来源。

## Java 源码规则（仅修改 `src/main/java/**/*.java` 时适用）

- 这是一个 Minecraft 1.21.1 NeoForge mod，Java 源码位于 `src/main/java`，目标 Java 21、NeoForge 21.1.235。
- 只改当前请求相关的代码。将 `build/`、`bin/`、`run/`、`sources/`、`reference/`、`libs/` 视为只读，仅在需要时作为参考。
- 优先复用项目已有模式，不引入新抽象。
- 先读直接相关的类、最近的调用方或测试。需要确认外部 API 或参考实现时，使用 `minecraft-mod-source-lookup` 技能。
- 用 `./gradlew.bat classes` 验证 Java 改动，除非存在更窄的相关检查。

## 资源与数据规则（仅修改 `src/main/resources/**/*.{json,lang,mcmeta,png}` 时适用）

- 这是一个 Minecraft 1.21.1 NeoForge mod，资源与数据文件位于 `src/main/resources`。
- 只改当前请求直接需要的资源或数据。
- 保持现有命名空间、JSON 布局与模型约定。
- 将 `build/`、`bin/`、`run/`、`sources/`、`reference/`、`libs/` 视为只读参考目录，不要编辑。
