# 当前任务：飞机可操纵性调试 + 飞行数据记录工具（方案 B）

> 滚动记录，随进度更新。相关设计知识沉淀在 `.design_guide/aircraft.md`，aero_bearing 机制见 `my_bearing.md`。

## 1. 目标
定位用户在游戏（Simulated/Sable + ccpe 航电）里造的固定翼飞机的问题：
- **低空翘头冲高**、**高空（205 m → 重做后 300 m）缓慢沉浮振荡（phugoid）**；
- （次要）高速大俯仰角时副翼剧烈抖动 + "着地"声（未定位）。

手段 = Java 服务端**飞行数据记录器**（只读，不改物理），CSV 落盘后用 Python 分析。

## 2. 已确认的物理事实（改写进 .design_guide/aircraft.md 的依据）
- 游戏气动 ≠ 现实：力方向**机体固定**、大小 ∝ 局部气流速度 v（线性非 v²）、攻角效应只来自 **(n·v)** 项；
- **对称帆 = 风向标式 α 弹簧**：法向阻力 (n·v) 是奇函数 → 放重心后方即可**双向静稳定**，"升力中心在重心之后"规则是现实航空学的误移植，已在指南中改写（`α 敏感面（尾翼）vs 重心` 行）；
- 普通帆升力方向恒沿 +n（体固定）、大小随 |n·v|² 减小（偶函数）→ 机翼纵向位置只管配平、不提供双向刚度；
- 全部气动力矩 ∝ 气压 P = e^(−0.004·(y−海平面)) → 高空（300 m ≈ 0.39）刚度与阻尼自动减半以上。

## 3. 飞行数据记录器（已实现，可进游戏用）
- 主文件：`src/main/java/com/zzy205/myfirstmod/compat/cc/FlightDataRecorder.java`
- 入口：`CCPeripheralExtender` 挂 `ServerTickEvent.Post`；停止时 `closeAll()` + `BodySensorRegistry.clear()`
- 配置：`Config.flightDataRecorderEnabled`（默认 true）/ `flightDataRecorderIntervalTicks`（默认 1 = 20 Hz）
- 输出：`<gameDir>/flight_logs/flight_<维度>_<bodyUuid前8>_<起始tick>.csv`，**每个 FMC（含 AIC）机体一个文件**；表头只在建文件时写 → **改代码后必须重启游戏再飞**
- CSV 列分组（顺序见 HEADER）：
  - 运动学/环境：tick、time_s、机体位姿（x/y/z、四元数）、pitchDeg/rollDeg/yawDeg、世界线速度 + |v|、世界角速度 w、机体系角速度 wb、静压孔平均 pressure/altitude、皮托管 airSpeed/groundSpeed、massKg/chainMassKg、com（世界 + 相对原点）、chainComRel
  - 受力（主机自身）：lift/drag/prop × (Fx,Fy,Fz,Mx,My,Mz) —— Sable 力组点力重算，机体局部系，M 绕该体 plot 质心
  - 受力（**整链**）：chainLift/chainDrag/chainProp × 同上 —— 链上全部 ServerSubLevel（含 aero_bearing 从动尾翼/副翼）的点力转世界系求和、绕**主机世界质心**求矩、再转回主机局部系（图纸看不到的部分就在这）
  - 控制输入：joyCh/joyX/joyY/joyXA/joyYA（摇杆2 频道 7）、thrCh/thrAxis/thrGear/thrFwd/thrBack（油门 频道 8）、pedCh/pedL/pedR（脚踏板 频道 6）——频道常量在 FlightDataRecorder 顶部，接线变了改这里
- 配套改动：`SableCompat.getLinearVelocity()`、`SableCompat.getCenterOfMassPlot()`、`BodySensorRegistry.fmcBodies(server)`/`clear()`
- **机制要点**：
  - 力组只在 `enableIndividualQueuedForcesTracking(true)` 时被 Sable 填充——记录器每 tick 对主机**整条约束链**幂等开启、文件关闭时对当前链恢复 false（与 Simulated 图纸同款开关；同开图纸时记录器关闭会连带关掉图纸的）
  - 力组按注册表 id 匹配（sable:force_groups 的 lift/drag/propulsion），未直接引用 `ForceGroups` 类（其字段类型引用 veil `RegistryObject`，不在编译 classpath）
  - 采样 = 游戏 tick 末，读到的是该 tick **最后一个物理子步**的力组；单位是 Sable 每物理步冲量刻度 → 看趋势/力矩平衡，勿当绝对牛顿值
  - 全 nan 排查：先确认重启过；再查注册表匹配/跟踪开关
- 分析脚本（Python，纯 csv 模块）：`run/flight_logs/_analyze_flight.py`、`_analyze_flight2.py`

## 4. 符号约定（解读数据前必读）
- **pitchDeg：低头 = 正、抬头 = 负**（用户实测 `ss.getAngles()`）——⚠ 与 `SensorSystemAPI.computeAttitudeDeg` 代码注释"pitch 正 = 抬头"矛盾，待进游戏对照订正（注释或约定二选一）；
- roll/yaw、wb 均机体系；CSV 与 `ss.getAngles()` 同一公式同源。

## 5. 已分析数据结论（flight_overworld_00b1000b_21.csv，重做后的飞机，300 m）
- 巡航段（y 245–355）：p50 pitch −0.6°、空速 61 m/s、压力 p50 0.396、垂速 ±11 m/s；
- **300 m 是轻阻尼 phugoid**：周期 12–44 s（稳定后 ~25–30 s，理论 ~2πv/(g√2)≈27 s）；峰谷幅值 75→22 m，衰减率 ≈0.009/s（幅值减半 ~77 s）；俯仰-爬升率相关 −0.81（机身跟航迹 = 风向标行为，非发散）；
- 尾段 pitch +40°、空速掉到 33 是降落俯冲，分析时剔除（t≈164 s 后）；
- 结论：**静稳定 OK（尾帆 α 弹簧有效），问题是阻尼不足** + 残余抬头力矩 ∝ P 随高度漂移（推力线在重心之上的结构问题仍在）。

## 6. 待办 / 下一步
- [ ] **重启游戏 → 飞新航线**（300 m 松手 60 s + 一次爬升 + 落地），验证控制输入列与 chain 力列非空，发新 CSV
- [ ] 用新 CSV 做整链力矩分析：残余俯仰力矩 vs 气压（低空抬头源定量）、chainPropM（推力线偏置）、控制面贡献 = chain − 主机列
- [ ] （等用户确认）`startup.lua` 加**俯仰人工阻尼环**（INS 角速度反馈 → bearing_pitch，符号按"低头+、抬头−"取反）——治本 phugoid
- [ ] 副翼抖动着地声问题：未定位，需用户提供（几何干涉/轴承限位/驱动源/离地高度）
- [ ] pitch 正负约定：代码注释 vs 实测矛盾，进游戏对照后订正
- [ ] （可选）记录 monitor 上的引擎/auto_roll/auto_alt 开关与目标高度旋钮（现只在 Lua 侧）

## 7. 相关文件速查
| 用途 | 路径 |
|---|---|
| 记录器 | `src/main/java/com/zzy205/myfirstmod/compat/cc/FlightDataRecorder.java` |
| 控制台频道/模块状态 | `block/ControlDeskBlockEntity.java`（getJoystick2AxisX / getThrottleAxis / getPedalLeftAxis 等） |
| 力组 API | `api/sable-common-1.21.1-2.0.3-sources/.../api/physics/force/{QueuedForceGroup,ForceTotal,ForceGroups}.java`、`sublevel/ServerSubLevel.java`（enableIndividualQueuedForcesTracking） |
| 物理读取 | `compat/sable/SableCompat.java`、`compat/cc/BodySensorRegistry.java` |
| 设计指南 | `.design_guide/aircraft.md`（游戏 vs 现实气动、对称帆 α 弹簧、机动/稳定清单） |
| 分析脚本 | `run/flight_logs/_analyze_flight.py`、`_analyze_flight2.py`、`_analyze_flight3.py`、`_analyze_flight4.py`、`_analyze_flight5.py` |
| 参考数据 | `run/flight_logs/flight_overworld_00b1000b_21.csv`（旧，仅运动学）、`flight_overworld_00b1000b_20.csv`（新，全列） |

---

## 8. 分析结论：flight_overworld_00b1000b_20.csv（新全列版，重启后录制）
> 129 s / 2580 行。控制输入列与整链力列**确认可用**（joyCh=7/thrCh=8/pedCh=6 全有值；巡航 ~70% 行有力数据）。
> 这趟飞行较"野"（pitch −52°~+82°、空速 −12~67 m/s、冲到 371 m 又俯冲 200 m），干净分析取**松手段 t 46.6–87 s（y 274–305，824 行）**。

### 8.1 工具与布局确认
- **chainLift == lift、chainProp == prop**（统计一致）→ 机翼（升力）与螺旋桨全在主机体；
- **chainDrag ≠ drag**：`|chainDragF|` p50 2.36 vs `|dragF|` 1.62，chainDragMx 尖峰 ±70 vs ±9 → **尾翼/副翼阻力贡献已被整链列记录**（图纸看不到的部分有了）。

### 8.2 轴与阻尼（干净段）
- **wbX = 俯仰速率**（与 pitch 数值导数相关 0.999）→ 后续直接用 wbX 当 pitch rate；力矩列 **Mx = 俯仰轴力矩**；
- 力矩 vs 俯仰速率相关：chainLiftMx −0.51、chainDragMx −0.55、**净 Mx −0.78** → 整机俯仰力矩在对抗俯仰转动（阻尼/刚度自洽）；
- **phugoid 仍在**：周期 **15.4 s**，峰 303→304→305、谷 276→274，**3 个周期幅值不衰减甚至微涨（27→30 m）** → 有阻尼但不足以耗散，且有持续能量输入（候选：满油门恒定 + 推力线偏置、auto_roll/auto_alt 隐形动舵、约束/伺服）。

### 8.3 关键发现：恒定残余俯仰力矩 ≈ +8
干净松手均值：chainLiftMx **+7.72**、chainDragMx −0.18、chainPropMx +0.58 → **净 Mx ≈ +8.1 恒定非零**。
→ 必有一个**未进力组**的力矩在抵消它（候选：aero bearing 约束/伺服力矩、Sable 通用阻力、某组力矩参考点/符号与预期不符）。"残余抬头/低头力矩"确证存在且随飞行状态变化，但**来源定位需要一次符号标定**（净力矩正方向 = 抬头还是低头，CSV 内自洽但缺游戏内参照）。

### 8.4 下一步（用户待执行）
1. **关 auto_roll / auto_alt** → 引擎满油门 → 300 m 平飞完全松手 60 s，不做任何机动；
2. Lua 顺手打印 `bearing_pitch.getTargetAngle()`（确认松手时升降舵是否真 0°，排除 Lua 隐形配平）；
3. 发新 CSV → 量真实阻尼比 ζ、验证"低空抬头"是否推力线偏置（chainPropMx 现仅 +0.58，偏置疑似不大）。









. 建议的下一步实验（把干扰砍干净）
关掉 auto_roll / auto_alt 两个 toggle（它们会主动动舵面，属于"隐形输入"）→ 只留引擎满油门；
300 m 平飞完全松手 60 s（不碰摇杆/踏板），期间不要做任何机动；
落地后发 CSV + 告诉我：松手时 bearing_pitch 的目标角是不是 0（如果 Lua 在偷偷配平，我需要知道——最好这趟在 Lua 里顺手把 bearing_pitch.getTargetAngle() 打出来）。
拿到这趟数据我就能：① 量出真正的阻尼比（ζ）和是否需要人工阻尼环；② 把净力矩均值扣掉后看振荡段力矩-气压关系，确认"低空抬头力矩"是不是推力线偏置（chainPropMx 才 +0.58，偏置可能不大，需要验证）。

顺带：要不要我把这轮 _20 的分析结论（含轴确认、净