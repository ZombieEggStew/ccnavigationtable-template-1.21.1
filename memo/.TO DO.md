# Monitor 元件系统 — 进度

## ✅ 已完成

### 当前已实现的关键事实
- [x] 14×12 Monitor 网格、模块/屏幕 NBT、客户端同步、渲染与交互基础设施。
- [x] 模块和屏幕的 ID 在单个 Monitor 内共享 `0..9999` 命名空间；放置时分配最小空闲 ID。
- [x] 模块和屏幕复用 `MonitorModuleScreen`；类型专属配置通过每次新建的 `ModuleConfigSection` 实例保存。
- [x] 按钮、钮子开关、旋钮和可变尺寸屏幕均已实现基础交互；旋钮角度已持久化和同步。
- [x] 多 Monitor 客户端状态、动画缓存和 Outliner key 已按 `BlockPos` 隔离。
- [x] 屏幕字符/矩形渲染（位图字体 + 纯色 quad）+ 模块/屏幕完整 Lua API，详见 `instruction_temp.md` 与 `memo/record_screen_text.md`。
- [x] 钮子开关组合渲染 (ToggleSwitchItemRenderer + SimpleCustomRenderer)
- [x] CC:Tweaked Lua API（读写模块状态；屏幕文本/矩形渲染）
---

## 🔲 待完成

### 测试 Monitor：偏航角实装（双轴旋转）
- [x] 抽公共变换 helper（`PitchMonitorTransform`：渲染正向 / 检测逆向共用同一套枢轴常量与旋转顺序）
- [x] BER 加偏航渲染（facing → yaw → pitch）
- [x] 命中检测补偏航逆变换（facing逆 → yaw逆 → pitch逆）
- [x] 描边选择框补偏航（底座固定、头部偏航）
- [x] 碰撞体不实装 yaw（物理碰撞无所谓，仅玩家视角检测走精确几何）


## 📝 本次工作记录 / 注意事项

### 双轴旋转方案（facing + yaw + pitch）
- **旋转顺序**（PoseStack 后调为内层、先作用于顶点）：facing（方块中心，Y，外层）→ yaw（颈部，Y，中层）→ pitch（铰链，X，内层）。即 `T(center)·Ry(facing)·T(-center) → T(neck)·Ry(yaw)·T(-neck) → T(hinge)·Rx(pitch)·T(-hinge)`。
- **枢轴静态常量（供模型微调）**：`HINGE_Y=2/16`、`HINGE_Z=6/16`（俯仰铰链，绕 X）；`NECK_X=8/16`、`NECK_Z=6.5/16`（偏航颈部，case 中心，绕 Y）。
- ⚠️ 铰链 z=6 与颈部 z=6.5 差 0.5：yaw 时铰链绕颈部画小弧，微调模型时可对齐或保留。
- **yaw 语义**：相对 facing（yaw=0 即当前朝向）；底座固定（blockstate 90° 步进），头部（case + 棋盘）连续旋转。
- **命中检测**：世界/plot → 模型空间的逆变换顺序为 `facing逆 → yaw逆 → pitch逆`（正向的逆序、各轴取负）。
- **碰撞体**：不实装 yaw（物理碰撞无所谓，仅玩家视角检测走精确几何）。
- **万向锁**：不处理，模组介绍中提示玩家避免 pitch=±90° 相关表现。

### 需要注意的坑
- 测试 monitor 的 `getShape` 不要每帧动态重算（`VoxelShaper.forHorizontal` + 12 切片 `Shapes.or` 很贵，会造成视角对准时掉帧）；已改为按 (facing, pitch) 懒缓存，碰撞 `getCollisionShape` 只用底座。
- `GridState.trySetId` 改 ID 时必须同步 re-key：`modules`、`grid[][]`、`pressedModules`、`knobAngles`、`moduleConfigs`（新增字段时别漏）。
- section 实例由 `ModuleConfigSections` 工厂**每次新建**，不要做成单例（内部持有控件引用）。
- 屏幕(`name == "screen"`)和未注册类型都走 `ModuleConfigSection.Empty`，不进注册表。
- `MonitorBlockEntity` 必须重写 `getUpdatePacket()`（默认返回 null），否则 `sendBlockUpdated` 不会推 BE 数据到客户端。
- 扳手右键模块：交互检测需排除 `holdingWrench`，否则拆卸前会先触发一次按下。
- 模块 ID 命名空间已包含屏幕，`GridState.getOccupiedIds()` 返回二者并集，菜单滚轮跳过时要用它。
- tooltip 显示开关存于模块配置的 `showTooltip`；旧存档无此字段时按开启处理。
