In src/main/java/com/zzy205/myfirstmod/block/TrailingWheelMountBlockEntity.java:

> +    /** Lua 转向输入（-15..15，0=直线；服务端权威，不持久化，仅随更新包同步客户端） */
+    private int steeringSignal = 0;
+    /** 转向偏航角（chasing 平滑，客户端渲染 / 服务端施力共用，照 offroad chasingYaw） */
+    private double chasingYaw = 0.0, lastChasingYaw = 0.0;
+
     private final Vector3d queuedForcePos = new Vector3d();
     private final Vector3d queuedForce = new Vector3d();
     private final ForceTotal forceTotal = new ForceTotal();
These fields are written by setSteeringSignal/network handling on the server thread and read by the Sable physics callback plus CC Lua getters, which are separate execution contexts. Without volatile or synchronized publication, a steering command or telemetry update can be observed late or inconsistently. Publish the cross-thread state with volatile fields or a synchronized snapshot.

In src/main/java/com/zzy205/myfirstmod/compat/cc/FlightDataRecorder.java:

> +    public static void onServerTick(ServerTickEvent.Post event) {
+        if (!Config.FLIGHT_RECORDER_ENABLED.get()) {
+            closeAll();
+            return;
+        }
+        int interval = Config.FLIGHT_RECORDER_INTERVAL_TICKS.get();
+        if (interval <= 0) {
+            closeAll();
+            return;
+        }
+        MinecraftServer server = event.getServer();
+        long tick = server.getTickCount();
+        if (tick % interval != 0) return;
+
+        try {
+            List<ServerSubLevel> bodies = BodySensorRegistry.bodiesWithAny(server, SensorType.FMC, SensorType.ATTITUDE);
bodiesWithAny(server, FMC, ATTITUDE) returns one sublevel for every matching registry UUID, not one entry per connected physical chain. If an INS/AIC/FMC is installed on a constrained child, both the child and host are returned, so each gets its own UUID writer and sampleRow; one aircraft then produces duplicate logs, and closing one writer can also disable force tracking for the other. Deduplicate by a canonical connected-chain/body identity before opening and writing files.

In src/main/java/com/zzy205/myfirstmod/compat/cc/FlightDataRecorder.java:

> +            if (hasPitot && hasPort && lastPitot != null) {
+                // 修正世界系点速度/空速（裸读 Sable.HELPER.* 含幻影值）
+                Double g = axisSpeed(sub, lastPitot, SableCompat.getWorldPointVelocity(sub.getLevel(), sub, lastPitot.pos()));
+                Double a = axisSpeed(sub, lastPitot, SableCompat.getWorldAirVelocity(sub.getLevel(), sub, lastPitot.pos()));
entries comes from sensorsOnBody(sub) and therefore may include a pitot on a constrained child sub-level, but both point-velocity calls are evaluated with the main sub. The child tube's pose/lever arm and axis are then transformed as if they belonged to the main body, producing incorrect ground/air-speed columns (and potentially selecting the wrong block state). Preserve each sensor's owning sub-level in the registry entry, or resolve and use that sub-level for these calculations.

In src/main/java/com/zzy205/myfirstmod/compat/cc/SensorSystemAPI.java:

> +     * 静态全局缓存：多维度同时使用时取最后一次加载的维度曲线（与螺旋桨 T/A 静态缓存
+     * {@link #refreshAeroConfig()} 同源策略）；Lua 线程只读该快照做纯数学换算，线程安全。
These are process-wide caches, so loading an FMC/AIC in a second dimension replaces the curve used by every existing computer. A computer still flying in the first dimension will then convert altitude/pressure (and the dependent cruise calculation) with the second dimension's data; keep the resolved atmosphere keyed by dimension or attach it to each API instance instead of using the last-loaded global snapshot.

In src/main/java/com/zzy205/myfirstmod/compat/cc/SensorSystemAPI.java:

> +            pressureBase = base;
+            pressureAnchors = anchors;
+            atmosphereMinY = level.dimensionType().minY();
+            atmosphereMaxY = atmosphereMinY + level.dimensionType().logicalHeight();
The Lua-side readers treat these four volatile fields as one immutable curve snapshot, but they are published in separate writes. A worker thread can observe new anchors with the old base pressure or old Y bounds during refreshPressureCurve, yielding a transiently incorrect conversion or binary-search result. Publish a single immutable record containing base, anchors, and bounds through one volatile reference.

In src/main/java/com/zzy205/myfirstmod/compat/cc/SensorSystemAPI.java:

> +     * 时调用一次；不随每 tick 刷新。读取 {@code DimensionPhysicsData.getUniversalDrag}
+     * （服务端维度物理，回退链与 {@link #refreshPressureCurve(Level)} 相同）；读不到时保留上次缓存
+     * （默认 0.09）。
universalDragCoefficient has the same global-last-dimension behavior, but getUniversalDragForce and solveMaxCruise are exposed to every FMC. A custom universal_drag in another dimension silently changes force and cruise calculations here; keep this coefficient with the corresponding dimension snapshot rather than a single static value.

In src/main/java/com/zzy205/myfirstmod/compat/cc/SensorSystemAPI.java:

>      /** 皮托管沿管口朝向的<b>对地</b>速度分量（m/s，有符号） */
     private @Nullable Double computeSpeed(SubLevel sub, BlockPos sensorPos) {
-        return axisSpeed(sub, sensorPos, SableCompat.getVelocity(sub.getLevel(), sensorPos));
+        return axisSpeed(sub, sensorPos, SableCompat.getWorldPointVelocity(sub.getLevel(), sub, sensorPos));
     }
 
     /** 皮托管沿管口朝向的<b>空速</b>分量（相对空气，已减风速，m/s，有符号） */
     private @Nullable Double computeAirSpeed(SubLevel sub, BlockPos sensorPos) {
-        return axisSpeed(sub, sensorPos, SableCompat.getAirVelocity(sub.getLevel(), sensorPos));
+        return axisSpeed(sub, sensorPos, SableCompat.getWorldAirVelocity(sub.getLevel(), sub, sensorPos));
lastPitot() can return a tube from any connected sub-level, but these methods always pass the computer's sub to both point-velocity and axis transforms. A pitot mounted on a constrained child therefore gets the wrong pose/lever arm and orientation, so getSpeed()/getAirSpeed() report incorrect values. Retain the owning sub-level with each sensor entry and use it for the velocity and axis calculation instead of silently accepting the documented boundary case.

In src/main/java/com/zzy205/myfirstmod/compat/cc/TrailingWheelMountPeripheral.java:

> +        if (other == this) return true;
+        if (other instanceof TrailingWheelMountPeripheral that) {
+            return this.be.getBlockPos().equals(that.be.getBlockPos());
+        }
Peripheral equality only compares BlockPos, so two trailing-wheel peripherals at the same coordinates in different dimensions are considered equal. CC:Tweaked can then reuse the cached peripheral object across dimensions and route calls to the wrong block entity. Include the owning level/dimension in the equality check.

In .design_guide/analysis/_analyze_flight6.py:

> +ext = sorted(peaks+trs, key=lambda p:p[0])
+per = [ext[k+1][0]-ext[k][0] for k in range(len(ext)-1)]
+print(f"half-periods: {[round(p,1) for p in per]}")
+print(f"full period mean: {2*sum(per)/len(per):.1f} s")
A valid short or strongly damped flight can produce no alternating extrema, leaving per empty; this unconditional sum(per) / len(per) then raises ZeroDivisionError and prevents the force/moment analysis from running. Guard the period calculation and print an unavailable-period result when fewer than two extrema exist.

In .design_guide/analysis/_verify_pressure.py:

> +# high-altitude detail from _8_delaytest (y up to 361)
+print("\n-- y>270 detail from flight_overworld_00b1000b_8_delaytest.csv --")
+rows = load(os.path.join(LOG_DIR, "flight_overworld_00b1000b_8_delaytest.csv"))
+hi = [r for r in rows if r[0] > 270]
+step = max(1, len(hi)//12)
+for y, p in hi[::step]:
+    print(f"   y={y:7.2f}  rec={p:.5f}  P0={P0(y):.5f}  P1={P1(y):.5f}  rec-P0={p-P0(y):+.5f}  rec-P1={p-P1(y):+.5f}")
This unconditional load makes the analysis script fail with FileNotFoundError on a normal installation that has flight logs but not this one historical _8_delaytest file. The script should treat the detail file as optional (or select an available log) instead of requiring a hard-coded developer artifact.

In src/main/java/com/zzy205/myfirstmod/compat/cc/BodySensorRegistry.java:

> +    public static List<ServerSubLevel> bodiesWithAny(MinecraftServer server, SensorType... types) {
+        List<ServerSubLevel> out = new ArrayList<>();
+        if (server == null || types.length == 0) return out;
+        for (Map.Entry<UUID, Set<SensorEntry>> entry : SENSORS.entrySet()) {
+            if (!hasAny(entry.getValue(), types)) continue;
+            for (ServerLevel level : server.getAllLevels()) {
This enumerates one ServerSubLevel per sensor-registry UUID, but a constrained assembly can have sensors registered on different sub-levels. bodiesWithAny will then return multiple members of the same connected chain, causing FlightDataRecorder (which keys writers by sub-level UUID) to create duplicate CSVs and sample the same physical body multiple times. Return one canonical sub-level per connected chain, or deduplicate by the chain identity before exposing the list.

In src/main/java/com/zzy205/myfirstmod/compat/cc/FlightDataRecorder.java:

> +            sub.enableIndividualQueuedForcesTracking(true);
+            this.trackingEnabled = true;
+            Path dir = FMLPaths.GAMEDIR.get().resolve("flight_logs");
+            Files.createDirectories(dir);
If directory creation, file creation, or header writing fails after line 198 enables force tracking, onServerTick only catches the constructor's IOException and never calls close(). That leaves Sable individual-force tracking enabled on a body even though no writer was installed; set up the file first or cleanly disable tracking on every partial-construction failure.

In src/main/java/com/zzy205/myfirstmod/compat/cc/FlightDataRecorder.java:

> +            // 后缀用保存时间（文件创建时刻，yyyyMMdd-HHmmss）而非起始 tick：
+            // 旧命名按起始 tick 会在重启后同机体同 tick 覆盖旧文件，时间戳不会重复
+            String ts = DateTimeFormatter.ofPattern("yyyyMMdd-HHmmss").format(LocalDateTime.now());
+            Path file = dir.resolve(String.format(Locale.ROOT, "flight_%s_%s_%s.csv", dim, id8, ts));
+            this.out = Files.newBufferedWriter(file, StandardCharsets.UTF_8);
The filename has only second-level timestamp precision, while newBufferedWriter uses the default truncate-existing behavior. If a body is unloaded/reloaded or its level object is replaced twice within one second, this path is reused and the earlier CSV is silently overwritten, contrary to the documented no-overwrite behavior; use collision-safe CREATE_NEW creation with a retry/suffix.

In src/main/java/com/zzy205/myfirstmod/compat/cc/FlightDataRecorder.java:

> +            UUID body = sub.getUniqueId();
+            String id8 = body != null ? body.toString().replace("-", "").substring(0, 8) : "unknown";
+            // 后缀用保存时间（文件创建时刻，yyyyMMdd-HHmmss）而非起始 tick：
+            // 旧命名按起始 tick 会在重启后同机体同 tick 覆盖旧文件，时间戳不会重复
+            String ts = DateTimeFormatter.ofPattern("yyyyMMdd-HHmmss").format(LocalDateTime.now());
+            Path file = dir.resolve(String.format(Locale.ROOT, "flight_%s_%s_%s.csv", dim, id8, ts));
+            this.out = Files.newBufferedWriter(file, StandardCharsets.UTF_8);
+            out.write(String.join(",", HEADER));
+            out.newLine();
+        }
+
+        void writeRow(ServerSubLevel sub, long tick) {
+            try {
+                out.write(sampleRow(sub, tick));
+                out.newLine();
+                out.flush();
With the default interval of one tick, every active body performs a synchronous flush on the server tick thread. Disk latency or a slow filesystem can therefore stall ticks and reduce TPS as the number of recorded bodies grows; buffer rows and flush periodically/off-thread, or make the durability/performance trade-off explicit.

In src/main/java/com/zzy205/myfirstmod/compat/cc/FlightDataRecorder.java:

> +            SensorEntry lastPitot = null;
+            for (SensorEntry e : entries) {
+                switch (e.type()) {
+                    case PRESSURE -> {
+                        hasPort = true;
+                        Vec3 worldPos = SableCompat.projectOutOfSubLevel(sub.getLevel(), e.pos());
+                        if (worldPos != null) {
+                            Double pr = computePressure(sub.getLevel(), worldPos);
+                            if (pr != null) {
+                                sumP += pr;
+                                sumA += worldPos.y;
+                                count++;
+                            }
+                        }
+                    }
+                    case SPEED -> {
+                        hasPitot = true;
+                        lastPitot = e;
+                    }
lastPitot is selected from the concatenation returned by sensorsOnBody, but that list is grouped by the connected sub-level traversal, not by global registration/placement order (the registry is keyed by HashMap UUIDs). With multiple pitots on different linked sub-levels, the recorded tube can change nondeterministically and does not implement the documented “most recently placed” behavior. Preserve an explicit registration sequence/owner with each sensor, or define and document a deterministic selection rule.

In src/main/java/com/zzy205/myfirstmod/Config.java:

> +            .comment("Flight data recorder (debug): log per-tick body data for every FMC-registered aircraft into <gameDir>/flight_logs/*.csv "
+                    + "(see compat/cc/FlightDataRecorder.java). Only active while at least one FMC/AIC is on a Sable physics body. "
+                    + "Default OFF; enable in the config only when you need to record a flight.")
The config description says recording is active only when an FMC/AIC is present, but FlightDataRecorder explicitly enumerates SensorType.ATTITUDE as well and records INS-only bodies. This tooltip misleads users into thinking the recorder is broken when an INS-only body produces a log; document the FMC/AIC/INS scope here too.

In src/main/java/com/zzy205/myfirstmod/compat/cc/FlightDataRecorder.java:

> + * 服务端每个 ServerTick（可配置间隔）对<b>每个已注册 FMC（含 AIC）的物理体</b>采样一行，
+ * 写入 {@code <gameDir>/flight_logs/flight_<维度>_<bodyUuid前8>_<保存时间>.csv}（保存时间 = 文件创建时刻，yyyyMMdd-HHmmss）。
This class-level documentation says the recorder samples every FMC-registered body, but onServerTick also enumerates SensorType.ATTITUDE, so an INS-only body is recorded too. Please document the actual FMC/AIC-or-INS scope here; otherwise the source documentation contradicts the implementation and the recorder's CSV behavior.

In src/main/java/com/zzy205/myfirstmod/compat/cc/FlightDataRecorder.java:

> + * + <b>通用阻力</b>（univFx/y/z：整链 Rapier 线速度阻尼的每物理子步冲量，转主机局部系；
+ * 力组/图纸里都没有这项，但物理里恒存在——平衡时 prop+drag+lift+univ ≈ 0，计算见
+ * {@link #appendUniversalDrag}）
+ * + <b>控制输入</b>（自动扫描链上短程信号链接器频道空间的控制台，记录其已安装的
+ * 脚踏板 / 操纵杆1 / 操纵杆2 / 油门1 / 油门2，每类取链内第一台；频道列 = 该控制台真实频道，
+ * 数据源与各控制台模块句柄一致）。未安装该控件 / 链上无控制台时该组列写 0；
+ * 数值读取失败列写 {@code nan}；力组不存在（无对应力源）时力列为 nan。
+ * <p>
+ * <b>受力前提</b>：记录器对主机及其整条约束链（含 aero_bearing 从动 sub-level）调用
+ * {@code ServerSubLevel.enableIndividualQueuedForcesTracking(true)}（Simulated 图纸同款机制，
+ * 每 tick 幂等开启、文件关闭时对当前链恢复 false）；否则 LIFT/DRAG 组点力不会被 Sable 记录。
+ * 采样时机 = ServerTick（每游戏 tick 末），读到的是该 tick 最后一个物理步记录的力组（组在每个
+ * 物理步开始被 reset）；phugoid 级分析足够。力/力矩单位与 Sable 内部一致（每物理步冲量刻度）。
+ * <p>
+ * 只读 Sable / 控制台 BE 公开 API，不改任何物理或控制行为；开关/采样间隔见
+ * {@link Config#FLIGHT_RECORDER_ENABLED}（默认开，仅在有 FMC 物理体时产生文件）。
This Javadoc says the recorder is enabled by default, but Config.FLIGHT_RECORDER_ENABLED is defined with a default of false, and the config/wiki descriptions say it is off by default. Please change this sentence so source documentation does not tell users to expect files without enabling the config.

In src/main/resources/assets/ccpe/lang/en_us.json:

> @@ -172,6 +172,42 @@
   "ccpe.configuration.sensorMaxForceLoad.tooltip": "Maximum number of sensors that can force-load chunks simultaneously. 0 = unlimited.",
 
   "ccpe.configuration.sensorPortalTicketRadius": "Physics Ticket Radius",
-  "ccpe.configuration.sensorPortalTicketRadius.tooltip": "PORTAL ticket radius in chunks for sensors on Sable physics structures. Each sensor loads (2×R+1)² chunks. Default: 3 (7×7 chunks)."
+  "ccpe.configuration.sensorPortalTicketRadius.tooltip": "PORTAL ticket radius in chunks for sensors on Sable physics structures. Each sensor loads (2×R+1)² chunks. Default: 3 (7×7 chunks).",
+
+  "ccpe.configuration.servoStressImpact": "Transmission Mode Stress Coefficient",
+  "ccpe.configuration.servoStressImpact.tooltip": "Stress coefficient for the Transmission Peripheral in transmission mode. Actual stress = value × |output RPM − input RPM|.",
+
+  "ccpe.configuration.servoModeStressImpact": "Servo Mode Stress Coefficient",
+  "ccpe.configuration.servoModeStressImpact.tooltip": "Stress coefficient for the Transmission Peripheral in servo mode. Actual stress = value × |real output RPM| (the output speed may be overridden/accelerated by setServoSpeed).",
+
+  "ccpe.configuration.flightDataRecorderEnabled": "Flight Data Recorder",
+  "ccpe.configuration.flightDataRecorderEnabled.tooltip": "Log per-tick body data for every FMC-registered aircraft into <gameDir>/flight_logs/*.csv (debug tool). Only active while at least one FMC/AIC is on a Sable physics body. Default OFF; enable only when you need to record a flight.",
This translated tooltip repeats the old FMC/AIC-only scope, while the recorder also samples INS-only bodies via SensorType.ATTITUDE. Users enabling this setting for an INS-only diagnostic body will be told it is inactive even though a CSV is written.

In src/main/resources/assets/ccpe/lang/zh_cn.json:

>  
+  "ccpe.configuration.servoStressImpact": "变速箱模式应力系数",
+  "ccpe.configuration.servoStressImpact.tooltip": "传动外设在变速箱模式下的应力系数。实际应力 = 系数 × |输出转速 − 输入转速|。",
+
+  "ccpe.configuration.servoModeStressImpact": "舵机模式应力系数",
+  "ccpe.configuration.servoModeStressImpact.tooltip": "传动外设在舵机模式下的应力系数。实际应力 = 系数 × |实际输出转速|（输出转速可能被 setServoSpeed 覆盖/加速）。",
+
+  "ccpe.configuration.flightDataRecorderEnabled": "飞行数据记录器",
+  "ccpe.configuration.flightDataRecorderEnabled.tooltip": "（调试）为每个注册了 FMC 的机体按 tick 记录数据到 <gameDir>/flight_logs/*.csv。仅在机体（含约束链）上有 FMC/AIC 时工作。默认关闭；仅在需要录制飞行时开启。",
此提示仍写成只有 FMC/AIC 才记录，但记录器也会通过 SensorType.ATTITUDE 采样仅有 INS 的物理体；用户会被误导为记录器未工作。请在这里同步说明 FMC/AIC/INS 的范围。

In wiki/docs/sensor-system/flight-data-recorder.md:

> +- Files are produced only while **at least one FMC/AIC is on a Sable physics body**.
+- The recorder also covers bodies with only an INS (ATTITUDE) sensor: the kinematics/velocity diagnostic columns are always available; without an FMC the physics-data columns read `nan`.
This sentence contradicts the next bullet and the implementation, which passes both FMC and ATTITUDE to bodiesWithAny and therefore records INS-only bodies. Please include INS in this condition (or remove INS-only recording) so the English documentation describes when files are actually produced.

In wiki/docs/sensor-system/flight-data-recorder.zh.md:

> +- 只有**至少一个 FMC/AIC 在某个 Sable 物理体上**时才会产生文件。
+- 记录器同样覆盖仅装有 INS（ATTITUDE）的机体：运动学/速度诊断列始终可用，无 FMC 时物理数据列为 `nan`。
这句话与下一条及实现不一致：实现把 FMC 和 ATTITUDE 都传给 bodiesWithAny，因此仅有 INS 的物理体也会记录。请在这里把 INS 纳入产生文件的条件（或移除 INS-only 记录），避免中文文档与实际行为矛盾。